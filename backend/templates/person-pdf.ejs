<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= person.name || 'Profile' %> - Detailed Profile</title>
    <style>
        @page {
            size: A4;
            margin: 0.2in 0.3in;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.4;
            color: #333;
            background: white;
        }
        
        .profile-container {
            max-width: 100%;
            margin: 0 auto;
            padding: 15px;
        }
        
        /* Header Section */
        .header {
            text-align: center;
            margin-bottom: 25px;
            border-bottom: 3px solid maroon;
            padding-bottom: 15px;
        }
        
        .company-name {
            font-size: 28px;
            font-weight: bold;
            color: maroon;
            margin-bottom: 8px;
            letter-spacing: 1px;
        }
        
        .company-details {
            font-size: 12px;
            color: #666;
            margin-bottom: 5px;
        }
        
        .profile-title {
            font-size: 22px;
            color: #2c3e50;
            margin-top: 15px;
            font-weight: 600;
        }
        
        /* Main Content Layout */
        .main-content {
            display: grid;
            grid-template-columns: 250px 1fr;
            gap: 25px;
            margin-bottom: 20px;
        }
        
        /* Profile Picture Section */
        .photo-section {
            text-align: center;
        }
        
        .profile-photo {
            width: 200px;
            height: 240px;
            border: 3px solid maroon;
            border-radius: 8px;
            object-fit: cover;
            margin-bottom: 15px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        
        .photo-placeholder {
            width: 200px;
            height: 240px;
            border: 2px dashed #ccc;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f8f9fa;
            color: #666;
            font-size: 14px;
            margin-bottom: 15px;
        }
        
        .photo-caption {
            font-size: 11px;
            color: #777;
            font-style: italic;
        }
        
        /* Details Sections */
        .details-container {
            flex: 1;
        }
        
        .section {
            margin-bottom: 20px;
            background: #fff;
            border: 1px solid #e0e0e0;
            border-radius: 6px;
            overflow: hidden;
        }
        
        .section-header {
            background: maroon;
            color: white;
            padding: 10px 15px;
            font-size: 14px;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .section-content {
            padding: 15px;
        }
        
        .detail-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 12px;
        }
        
        .detail-item {
            display: flex;
            padding: 8px 0;
            border-bottom: 1px solid #f0f0f0;
        }
        
        .detail-item:last-child {
            border-bottom: none;
        }
        
        .detail-label {
            font-weight: 600;
            color: #2c3e50;
            width: 140px;
            flex-shrink: 0;
            font-size: 12px;
        }
        
        .detail-value {
            color: #555;
            flex: 1;
            font-size: 12px;
            word-wrap: break-word;
        }
        
        /* Full Width Items */
        .full-width {
            grid-column: 1 / -1;
        }
        
        /* Siblings Section */
        .siblings-list {
            margin-top: 10px;
        }
        
        .sibling-item {
            background: #f8f9fa;
            padding: 10px;
            margin-bottom: 8px;
            border-radius: 4px;
            border-left: 3px solid maroon;
        }
        
        .sibling-name {
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 4px;
        }
        
        .sibling-details {
            font-size: 11px;
            color: #666;
        }
        
        /* Footer */
        .footer {
            margin-top: 30px;
            text-align: center;
            padding-top: 15px;
            border-top: 2px solid #eee;
            font-size: 11px;
            color: #888;
        }
        
        /* Status Indicators */
        .status-positive {
            color: #27ae60;
            font-weight: 600;
        }
        
        .status-negative {
            color: #e74c3c;
            font-weight: 600;
        }
        
        .status-neutral {
            color: #f39c12;
            font-weight: 600;
        }
        
        /* Responsive adjustments for PDF */
        @media print {
            .profile-container {
                padding: 10px;
            }
            
            .main-content {
                gap: 20px;
            }
            
            .section {
                break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <!-- Header Section -->
        <div class="header">
            <div class="company-name"><%= companyName %></div>
            <div class="company-details">📞 <%= PhoneNo %> | 📧 <%= companyEmail %></div>
            <div class="company-details">📍 <%= companyAddress %></div>
            <div class="profile-title">Detailed Matrimonial Profile</div>
        </div>

        <!-- Main Content -->
        <div class="main-content">
            <!-- Photo Section -->
            <div class="photo-section">
                <% if (person.profilePicture) { %>
                    <img src="<%= baseUrl %><%= person.profilePicture %>" alt="<%= person.name %>'s Photo" class="profile-photo">
                <% } else { %>
                    <div class="photo-placeholder">
                        📷<br>Photo Not Available
                    </div>
                <% } %>
                <div class="photo-caption">Profile Picture</div>
            </div>

            <!-- Details Container -->
            <div class="details-container">
                <!-- Personal Information -->
                <div class="section">
                    <div class="section-header">Personal Information</div>
                    <div class="section-content">
                        <div class="detail-grid">
                            <div class="detail-item">
                                <div class="detail-label">Full Name:</div>
                                <div class="detail-value"><%= person.name %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Gender:</div>
                                <div class="detail-value"><%= person.gender %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Date of Birth:</div>
                                <div class="detail-value"><%= person.dob %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Marital Status:</div>
                                <div class="detail-value"><%= person.maritalStatus %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Height:</div>
                                <div class="detail-value"><%= person.height %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Complexion:</div>
                                <div class="detail-value"><%= person.complexion %></div>
                            </div>
                            <div class="detail-item full-width">
                                <div class="detail-label">Birth Place & Time:</div>
                                <div class="detail-value"><%= person.birthPlaceTime %></div>
                            </div>
                            <div class="detail-item full-width">
                                <div class="detail-label">Native Place:</div>
                                <div class="detail-value"><%= person.nativePlace %></div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Religious & Cultural Details -->
                <div class="section">
                    <div class="section-header">Religious & Cultural Details</div>
                    <div class="section-content">
                        <div class="detail-grid">
                            <div class="detail-item">
                                <div class="detail-label">Religion:</div>
                                <div class="detail-value"><%= person.religion %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label">Gotra:</div>
                                <div class="detail-value"><%= person.gotra %></div>
                            </div>
                            <div class="detail-item full-width">
                                <div class="detail-label">Horoscope:</div>
                                <div class="detail-value"><%= person.horoscope %></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Contact Information -->
        <div class="section">
            <div class="section-header">Contact Information</div>
            <div class="section-content">
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Phone Number:</div>
                        <div class="detail-value"><%= person.phoneNumber %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">NRI Status:</div>
                        <div class="detail-value">
                            <% if (person.nri) { %>
                                <span class="status-positive">Yes</span>
                            <% } else { %>
                                <span class="status-negative">No</span>
                            <% } %>
                        </div>
                    </div>
                    <div class="detail-item full-width">
                        <div class="detail-label">Current Residence:</div>
                        <div class="detail-value"><%= person.residence %></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Educational Background -->
        <div class="section">
            <div class="section-header">Educational Background</div>
            <div class="section-content">
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Higher Qualification:</div>
                        <div class="detail-value"><%= person.higherQualification %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Graduation:</div>
                        <div class="detail-value"><%= person.graduation %></div>
                    </div>
                    <div class="detail-item full-width">
                        <div class="detail-label">Schooling:</div>
                        <div class="detail-value"><%= person.schooling %></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Professional & Financial Details -->
        <div class="section">
            <div class="section-header">Professional & Financial Details</div>
            <div class="section-content">
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Occupation:</div>
                        <div class="detail-value"><%= person.occupation %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Personal Income:</div>
                        <div class="detail-value"><%= person.personalIncome %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Family Income:</div>
                        <div class="detail-value"><%= person.familyIncome %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Vehicle:</div>
                        <div class="detail-value"><%= person.vehicle %></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Family Background -->
        <div class="section">
            <div class="section-header">Family Background</div>
            <div class="section-content">
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Father's Name:</div>
                        <div class="detail-value"><%= person.fatherName %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Father's Occupation:</div>
                        <div class="detail-value"><%= person.fatherOccupation %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Mother's Name:</div>
                        <div class="detail-value"><%= person.motherName %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Mother's Occupation:</div>
                        <div class="detail-value"><%= person.motherOccupation %></div>
                    </div>
                    <div class="detail-item full-width">
                        <div class="detail-label">Father's Office:</div>
                        <div class="detail-value"><%= person.fatherOffice %></div>
                    </div>
                    <div class="detail-item full-width">
                        <div class="detail-label">Other Property:</div>
                        <div class="detail-value"><%= person.otherProperty %></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Lifestyle & Preferences -->
        <div class="section">
            <div class="section-header">Lifestyle & Preferences</div>
            <div class="section-content">
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Eating Habits:</div>
                        <div class="detail-value">
                            <% if (person.eatingHabits && person.eatingHabits.toLowerCase().includes('veg')) { %>
                                <span class="status-positive"><%= person.eatingHabits %></span>
                            <% } else { %>
                                <%= person.eatingHabits %>
                            <% } %>
                        </div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Drinking Habits:</div>
                        <div class="detail-value">
                            <% if (person.drinkingHabits && person.drinkingHabits.toLowerCase().includes('no')) { %>
                                <span class="status-positive"><%= person.drinkingHabits %></span>
                            <% } else { %>
                                <%= person.drinkingHabits %>
                            <% } %>
                        </div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Smoking Habits:</div>
                        <div class="detail-value">
                            <% if (person.smokingHabits && person.smokingHabits.toLowerCase().includes('no')) { %>
                                <span class="status-positive"><%= person.smokingHabits %></span>
                            <% } else { %>
                                <%= person.smokingHabits %>
                            <% } %>
                        </div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Disability:</div>
                        <div class="detail-value">
                            <% if (person.disability && person.disability.toLowerCase().includes('no')) { %>
                                <span class="status-positive"><%= person.disability %></span>
                            <% } else { %>
                                <%= person.disability %>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Siblings Information -->
        <% if (person.siblings && person.siblings.length > 0) { %>
        <div class="section">
            <div class="section-header">Siblings Information</div>
            <div class="section-content">
                <div class="siblings-list">
                    <% person.siblings.forEach((sibling, index) => { %>
                        <div class="sibling-item">
                            <div class="sibling-name">
                                <%= index + 1 %>. <%= sibling.name || 'Name not provided' %>
                            </div>
                            <div class="sibling-details">
                                <% if (sibling.age) { %>Age: <%= sibling.age %> | <% } %>
                                <% if (sibling.occupation) { %>Occupation: <%= sibling.occupation %> | <% } %>
                                <% if (sibling.maritalStatus) { %>Status: <%= sibling.maritalStatus %><% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Additional Photos -->
        <% if (person.photos && person.photos.length > 1) { %>
        <div class="section">
            <div class="section-header">Additional Photos</div>
            <div class="section-content">
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(120px, 1fr)); gap: 15px; text-align: center;">
                    <% person.photos.slice(1, 4).forEach((photo, index) => { %>
                        <div>
                            <img src="<%= baseUrl %><%= photo %>" alt="Photo <%= index + 2 %>" 
                                 style="width: 120px; height: 140px; object-fit: cover; border: 2px solid #ddd; border-radius: 4px;">
                            <div style="font-size: 10px; color: #777; margin-top: 5px;">Photo <%= index + 2 %></div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </div>
        <% } %>

        <!-- Footer -->
        <div class="footer">
            <div style="margin-bottom: 8px;">
                <strong><%= companyName %></strong> - Your Trusted Matrimonial Partner
            </div>
            <div>
                Generated on <%= currentDate %> | Contact: <%= PhoneNo %> | Email: <%= companyEmail %>
            </div>
            <div style="margin-top: 8px; font-size: 10px;">
                This profile is confidential and intended for matrimonial purposes only.
            </div>
        </div>
    </div>
</body>
</html>